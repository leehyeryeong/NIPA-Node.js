const animal = 'Cat';
const legs = 4;
const sound = 'meow';

/* 지시사항의 내용을 따라 아래 코드를 수정해보세요. */
const explain = `${animal} has ${legs} legs.
                 ${animal} says "${sound}".`;
    
console.log(explain);
